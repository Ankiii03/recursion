#include<stdio.h>
#include<string.h>
int main()
  {       char str[40];
 // gets(str);
  scanf("%[^\n]s",str);
 // scanf("%c",&str[]);
      //  printf("%s",str);
      printf("%s",str);
     // puts(str);
            return 0;
  }